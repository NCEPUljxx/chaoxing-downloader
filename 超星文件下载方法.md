# 超星（学习通）课件下载方法

> 本文档记录超星课件下载助手的核心技术原理，涵盖页面结构、API 端点、CDN 路径、下载策略等。对应扩展版本 v7.0.0。

---

## 一、页面类型与结构

超星课程有两类关键页面：

### 1.1 资料区页面

**URL 特征**：`mooc2-ans.chaoxing.com/coursedata/stu-datalist` 或 `mooc2-ans.chaoxing.com/mooc2-ans/mycourse/stu`

**文件列表 DOM**（v7 新版结构）：

```html
<div role="option">
  <!-- 可下载文件：dataBody_check -->
  <li class="dataBody_check selectClass">
    <input type="checkbox" name="checkdelete" value="1317954032">
  </li>
  <!-- 文件名行：dataBody_file，含 toOpen -->
  <li class="dataBody_file" onclick="toOpen('文件名.docx','docx',1317954032,'','0720b70e...','',2,1,...)">
    <img src="/mooc2-ans/images/coursedata/word.png">
  </li>
  <!-- 下载链接 / 云盘链接 -->
  ...
</div>

<!-- 受限文件同样结构，但带 dataBody_disabled -->
<div role="option">
  <li class="dataBody_disabled">
    <input type="checkbox" name="checkdelete" value="1317954001">
  </li>
  <li class="dataBody_file" onclick="toOpen('超星课件.pdf','pdf',1317954001,...)">
    ...
  </li>
</div>
```

**关键信息提取**：

```javascript
// onclick 格式
toOpen('urlencodedName', 'type', dataId, '', 'objectId', '', flag, ...)

// 参数提取（页面 hidden input）
<input id="courseId"  value="255199023">
<input id="classId"   value="127363756">
<input id="cpi"       value="453227930">

// 权限判断
可下载：父级含 .dataBody_check → 直接下载 API 可用
受限：  父级含 .dataBody_disabled → 需要走预览解析
```

**旧版结构（兼容保留）**：文件列表在 `iframe[name="frame_content-zl"]` 中，结构类似。

### 1.2 课程学习页面

**URL 特征**：`mooc1.chaoxing.com/mycourse/studentstudy?chapterId=...&courseId=...`

**DOM 结构**：

```
主页面
├── iframe[src*="knowledge/cards"]  ← 课件容器
│   ├── iframe[objectid="b86bd0c..."] ← 知识点视频
│   │   属性: data='{"objectid":"...","name":"测量仪表的性能指标.mp4",...}'
│   │   src: ananas/modules/video/index.html
│   │
│   └── iframe[objectid="1f1e18e8..."] ← PDF 课件
│       属性: data='{"objectid":"...","name":"第一章 仪表指标.pdf",...}'
│       src: ananas/modules/pdf/index.html
│       └── iframe[src*="screen/file?objectid=...&ext=..."] ← 预览 iframe
│
├── 左侧目录树（提取章节名）
│   .catalog_section .current → "性能指标"
│
└── URL 参数: chapterId, courseId, clazzid, cpi, enc
```

**课件 iframe 的 `data` JSON 结构**：

```json
{
  "objectid": "1f1e18e8a0fa839299f5a9b4f67dcc13",
  "name": "第一章 2 仪表指标-描述I-O关系.pdf",
  "type": ".pdf",
  "size": 1934399,
  "hsize": "1.84 MB",
  "title": "第一章 2 仪表指标"
}
```

**课程页文件特征**：
- `iframe` 带 `objectid` 属性（值为 32 位 hex）
- `iframe` 的 `data` 属性含 JSON，内嵌 `objectid`、`name`、`type`
- 无 `dataId`（不是资料页），下载走另一条路径

---

## 二、API 端点

### 2.1 预览 URL 获取

```
GET https://mooc2-ans.chaoxing.com/mooc2-ans/coursedata/get-preview-url
  ?dataId={dataId}
  &cpi={cpi}
  &clazzid={classId}
  &ut=s
  &courseid={courseId}

Headers:
  Referer: https://mooc2-ans.chaoxing.com/
  X-Requested-With: XMLHttpRequest

Response:
  { "url": "https://pan-yz.chaoxing.com/preview/v2/objectshowpreview.html?...", "status": true }
```

**用途**：获取文件预览页 URL。需要 `dataId`（资料页才有）。

### 2.2 直接下载 API

```
GET https://mooc1.chaoxing.com/coursedata/downloadData
  ?dataId={dataId}
  &classId={classId}
  &cpi={cpi}
  &courseId={courseId}
  &ut=s

Headers:
  Referer: https://mooc2-ans.chaoxing.com/
```

**行为**：
- 可下载文件：返回文件内容（Content-Type 为实际文件类型，body > 200 bytes）
- 受限文件：返回 JSON 错误信息或小字节内容
- 判断方式：检查 `Content-Type` 是否为 `application/json` 或 `text/javascript`，以及 `blob.size > 200`

### 2.3 课程页课件预览（Screen/File）

```
GET https://mooc1.chaoxing.com/mooc-ans/screen/file
  ?objectid={objectId}
  &ext={encodedExt}

Headers:
  Referer: https://mooc1.chaoxing.com/

行为：HTTP 302 → pan-yz.chaoxing.com/screen/v2/file_{objectId}?...
```

**`ext` 参数格式**（URL 编码的 JSON）：

```json
{"_from_":"260460541_139806548_292736277_b4311b5afe04fdc71ffcc53219c40251"}
```

由 `courseId_classId_userId_enc` 拼接而成。`userId` 从页面 hidden input 提取，`enc` 从 URL 参数或 iframe src 提取。

### 2.4 预览页（pan-yz）

预览页有两种入口：

| 来源 | URL 格式 |
|---|---|
| 资料页 | `pan-yz.chaoxing.com/preview/v2/objectshowpreview.html?objectid=...` |
| 课程页 | `pan-yz.chaoxing.com/screen/v2/file_{objectId}?ext=...` |

两个入口最终渲染的页面结构相似，都包含 CDN 缩略图 URL。

---

## 三、CDN 架构

### 3.1 域名

| 域名 | 用途 |
|---|---|
| `s3.cldisk.com` | 主 CDN 存储（文档文件） |
| `s2.cldisk.com` | 辅助 CDN（视频/部分文档） |
| `d0.cldisk.com` | 下载代理（需认证，不直接使用） |
| `p.cldisk.com` | 别名域名（少数文件） |

### 3.2 路径结构

```
https://s3.cldisk.com/sv-w{N}/{type}/{XX}/{XX}/{XX}/{objectId}/
├── thumb/1.png              ← 缩略图（页面预览用，无需认证）
├── thumb/2.png
├── ...
├── pdf/{objectId}.pdf      ← PDF 渲染版（超星 CDN 转码，无需认证 ★ 推荐 ★）
├── {objectId}.pdf           ← 原始 PDF 文件（无需认证）
├── {objectId}.docx          ← 原始 DOCX 文件（无需认证）
└── {objectId}.pptx          ← 原始 PPTX 文件（无需认证）
```

**路径说明**：
- `sv-w{N}`：CDN 服务器版本号，常见 w7、w8、w9
- `{type}`：`doc` 或 `video`
- `{XX}/{XX}/{XX}`：3 级 2 位 hex 目录（从 `objectId` 前 6 位派生，但实际值由 CDN 分配，需从缩略图 URL 提取）
- `{objectId}`：32 位 hex，文件的唯一标识

### 3.3 CDN 前缀提取

从预览页 HTML 中匹配缩略图 URL，正则覆盖 5 种模式：

```
模式1: 完整 URL 带类型
  /https:\/\/s([23])\.cldisk\.com\/sv-w(\d+)\/(doc|video)\/([a-f0-9]{2}\/[a-f0-9]{2}\/[a-f0-9]{2,})\//

模式2: 缩略图 URL（thumb/png 结尾）
  /https:\/\/s([23])\.cldisk\.com\/sv-w(\d+)\/(doc|video)\/([a-f0-9]{2}\/[a-f0-9]{2}\/[a-f0-9]{2,})\/thumb\//

模式3: PDF 子目录 URL
  /https:\/\/s([23])\.cldisk\.com\/sv-w(\d+)\/(doc|video)\/([a-f0-9]{2}\/[a-f0-9]{2}\/[a-f0-9]{2,})\/pdf\//

模式4: d0.cldisk.com 下载链接
  /https:\/\/d(\d)\.cldisk\.com\/download\/([a-f0-9]{32})/
  → 用 objectId 前6位拼出 pf（推测路径）

模式5: 其他 cldisk 子域名
  /https:\/\/([a-z0-9]+)\.cldisk\.com\/([^"'\s]*)/
```

提取结果示例：

```javascript
{
  host: 'https://s3.cldisk.com',
  sv: '8',
  type: 'doc',
  pf: '2a/8b/42'  // 3级 hex 目录前缀
}
```

### 3.4 下载 URL 构造

```
PDF 文件：
  {host}/sv-w{sv}/doc/{pf}/{oid}/pdf/{oid}.pdf     ← PDF 渲染版（推荐）
  {host}/sv-w{sv}/doc/{pf}/{oid}/{oid}.pdf         ← 原始版

非 PDF 文档：
  {host}/sv-w{sv}/doc/{pf}/{oid}/{oid}.{ext}        ← 原始格式（优先）
  {host}/sv-w{sv}/doc/{pf}/{oid}/pdf/{oid}.pdf     ← PDF 渲染版（回退）

变体：每个候选 URL 尝试 sv-w7、sv-w8、sv-w9 三个版本
```

---

## 四、完整下载流程

### 4.1 资料区文件下载

```
用户点击下载
    │
    ▼
┌─ 非受限文件？(file.dis === false)
│   ├─ 是 → 调用 downloadData API
│   │       ├─ 返回有效文件（blob > 200B, 非 JSON） → 直接下载 ✅
│   │       └─ 返回错误/小字节 → fallback ▼
│   └─ 否（受限文件，含 .dataBody_disabled）
│           │
│           ▼
│   Step 1: get-preview-url API
│           获取预览页 URL（需 dataId）
│           │
│           ▼
│   Step 2: Fetch 预览页 HTML
│           携带 Referer 头
│           │
│           ▼
│   Step 3: extractCdnPrefix()
│           从 HTML 提取 CDN 前缀（5 种正则模式）
│           │
│           ▼
│   Step 4: 构造候选 URL 列表
│           PDF → pdf 子目录 + 原格式
│           其他 → 原格式 + pdf 子目录
│           每个 × sv-w7/8/9 变体
│           │
│           ▼
│   Step 5: chrome.downloads.download()
│           返回第一个候选 URL 给浏览器下载
│           （CDN URL 无需 Cookie 认证）
```

### 4.2 课程页文件下载

```
用户点击下载（课程页文件 did === '0'）
    │
    ▼
Step 1: resolveUrlFromScreen()
        构造 screen/file URL（objectid + ext）
        Fetch → 跟随重定向到 pan-yz 页面
        │
        ▼
Step 2: 从 pan-yz 页面提取 CDN 前缀
        缩略图 URL → host + sv + pf
        │
        ▼
Step 3: 构造 PDF 下载 URL
        {host}/sv-w{sv}/{type}/{pf}/{oid}/pdf/{oid}.pdf
        │
        ▼
Step 4: chrome.downloads.download()
```

### 4.3 文件权限标记解析

| HTML 结构 | 含义 | 下载策略 |
|---|---|---|
| `li.dataBody_check` + `li.dataBody_file` | 可下载 | 先试 downloadData API，失败走预览 |
| `li.dataBody_disabled` + `li.dataBody_file` | 受限 | 直接走预览页解析 |
| 课程页 iframe `objectid` | 内嵌课件 | screen/file → pan-yz → CDN |

---

## 五、页面文件列表扫描策略

扩展采用三级扫描，按优先级依次尝试：

### 5.1 新版资料页扫描（scanDataPage）

```javascript
// 查询选择器
document.querySelectorAll('li.dataBody_file[onclick*="toOpen"]')
// 如果没有，尝试 DT 元素
document.querySelectorAll('dt[onclick*="toOpen"]')

// onclick 正则解析
/toOpen\('([^']*)','([^']*)',(\d+),'([^']*)','([^']*)'/

// 参数映射
m[1] = 文件名 (URL encoded)  → decodeURIComponent → 替换 + 为空格
m[2] = 文件类型 (pdf/docx/pptx/mp4...)
m[3] = dataId (数字)
m[5] = objectId (32位hex)

// 去重键 = dataId + '_' + objectId（按 dataId+objectId 去重，
// 因为同一课件在页面中可能有 LI 和 DT 两个 onclick 元素）

// 视频过滤：跳过 mp4/mp3/m4a/flv 类型
```

### 5.2 旧版资料页扫描（scanDataPageLegacy）

```javascript
// 进入 iframe
document.querySelector('iframe[name="frame_content-zl"]')

// iframe 内查询
doc.querySelectorAll('[onclick*="toOpen"]')

// 其余逻辑与新版相同
```

### 5.3 课程页扫描（scanCoursePage）

```javascript
// 进入主课件 iframe
document.querySelector('iframe[src*="knowledge/cards"]')

// 查找带 objectid 或 data JSON 的 iframe
doc.querySelectorAll('iframe[objectid], iframe[data*="objectid"]')

// 从 data 属性 JSON 提取
{
  objectid: "1f1e18e8a0fa839299f5a9b4f67dcc13",
  name: "第一章 仪表指标.pdf",
  type: ".pdf"
}

// ext 参数提取
// 从 screen/file iframe URL 中匹配 ext={...}
// 或从页面 hidden input + URL enc 构造
```

### 5.4 参数提取（extractParams）

```javascript
// Hidden input
courseId / courseid   → p.cid
classId  / clazzid    → p.cl
cpi                   → p.cpi
courseName            → courseName（仅用于 UI 显示）

// URL 参数 fallback
location.href.match(/courseid[=:](\d+)/i)
location.href.match(/clazzid[=:](\d+)/i)
location.href.match(/cpi[=:](\d+)/i)
```

### 5.5 章节名提取（课程页使用）

```javascript
// 优先级：
1. .catalog_section .current     ← 左侧目录高亮项
2. [class*="chapter_item"].current
3. h2 标题
4. URL chapterId 参数

// 清洗：去数字前缀、去通用占位文本（"章节详情"/"正文"等）
```

---

## 六、扩展架构

```
┌──────────────────────────────────────────────┐
│                  Content Script              │
│  content.js + styles.css                     │
│                                              │
│  ┌─────────┐  ┌──────────┐  ┌────────────┐  │
│  │ Scanner  │  │   UI     │  │ Download   │  │
│  │ (2s轮询) │  │ (浮动面板)│  │ (消息发送) │  │
│  └────┬─────┘  └──────────┘  └─────┬──────┘  │
│       │                             │         │
└───────┼─────────────────────────────┼─────────┘
        │                             │
        │  chrome.runtime             │ chrome.runtime
        │  .sendMessage               │ .sendMessage
        │  (getStats)                 │ (dl)
        ▼                             ▼
┌──────────────────────────────────────────────┐
│              Background Service Worker       │
│  background.js                               │
│                                              │
│  ┌─────────────────┐  ┌───────────────────┐  │
│  │ downloadOne()    │  │ executeDownload() │  │
│  │ (URL解析/选择)    │  │ (chrome.downloads)│  │
│  └────────┬────────┘  └───────────────────┘  │
│           │                                   │
│  ┌────────┴────────────────────────────┐     │
│  │ resolveUrlFromPreview()   ← 资料页   │     │
│  │ resolveUrlFromScreen()    ← 课程页   │     │
│  │ extractCdnPrefix()        ← 5种正则  │     │
│  └─────────────────────────────────────┘     │
└──────────────────────────────────────────────┘
```

### 消息协议

**Content → Background（下载请求）**：
```javascript
{
  action: 'dl',
  files: [
    { n: '文件名', t: 'pdf', did: '1317954001', oid: 'ebc890a4...', dis: true, ext: '...' }
  ],
  params: { cpi: '453227930', cid: '255199023', cl: '127363756', ref: 'https://...' }
}
```

**Background → Content（进度通知）**：
```javascript
{
  action: 'cxProg',
  data: {
    ev: 'prog' | 'ok' | 'fail' | 'done',
    f: '文件名', j: 0, total: 8, ok: 0, fail: 0,
    err: '错误信息'  // 仅 fail 事件
  }
}
```

**Popup → Content → Popup（统计查询）**：
```javascript
// Request
{ action: 'getStats' }

// Response
{ filesCount: 8, restrictedCount: 3, courseName: 'ddd' }
```

---

## 七、文件类型支持

| 类型 | 扩展名 | CDN 原格式 | CDN PDF 版 | 说明 |
|---|---|---|---|---|
| PDF | .pdf | ✅ | ✅ | 优先 PDF 渲染版（可靠性高） |
| Word 文档 | .docx | ✅ | ✅ | 优先原格式，回退 PDF 版 |
| PPT 演示 | .pptx | ✅ | ✅ | 优先原格式，回退 PDF 版 |
| Other | .doc/.ppt | 视情况 | ✅ | 统一走 PDF 渲染版 |

> ⚠️ 视频（MP4/MP3/M4A/FLV）下载功能已移除。

---

## 八、跨浏览器兼容性

| 特性 | Chrome | Edge | 说明 |
|---|---|---|---|
| Manifest V3 | ✅ | ✅ | 标准支持 |
| Service Worker | ✅ | ✅ | 同内核 |
| chrome.downloads | ✅ | ✅ | API 一致 |
| CSS `appearance` | ✅ | ✅ | 需显式 `-webkit-appearance:checkbox` |
| CSS `flex-shrink:0` | ✅ | ✅ | Edge 需要用 `!important` 锁定 |
| `host_permissions` | ✅ | ✅ | MV3 标准 |

**Edge 特殊情况**：资料页的 checkbox 在 flex 容器中可能被压缩为 0 宽度，需要 `!important` 显式锁定 `min-width`、`max-width`、`flex-shrink`、`flex-basis`。

---

## 九、参考资料

### toOpen 函数签名

```javascript
// 新版（11 参数）
toOpen(name, type, dataId, '', objectId, '', flag, ?, classId, enc, ?, ?, ?)

// 旧版（6 参数）
toOpen(name, type, dataId, '', objectId, '')
```

前 6 个参数位置稳定，扩展仅依赖这些字段。

### objectId 特征

- 32 位十六进制字符串（`[a-f0-9]{32}`）
- 全局唯一（同一 objectId 不会对应两个不同文件）
- 在 URL、HTML 属性、JSON、JS 变量中均有出现

### dataId 特征

- 纯数字字符串（如 `1317954001`）
- 资料页文件有 dataId，课程页文件没有
- 用于区分两种下载路径

### courseId / classId / cpi

- `courseId`：课程 ID（纯数字）
- `classId`：班级 ID（纯数字）
- `cpi`：课程-班级关联参数（纯数字）
- 三者从 hidden input 或 URL 参数提取
- 所有 API 调用都需要这三个参数

---

## 十、变更记录

### v7.0.0
- 重写页面扫描器，兼容新版资料页 DOM 和无 iframe 结构
- 新增课程学习页内嵌课件识别（knowledge/cards → objectid iframe）
- 新增 screen/file 端点下载路径（课程页文件）
- 新增章节名提取与使用（课程页下载时作为文件名）
- 文件列表去重改为 `dataId + objectId` 双键
- 视频下载功能移除（扫描和下载均过滤）
- Edge checkbox 不可见修复（CSS `!important` 锁定尺寸）
- 面板定位修复（读取坐标在隐藏按钮之前）
- 扩展 URL 匹配覆盖全部超星子域名
